import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "planned-marketing-git-get-started-gift-card-planned.vercel.app",
      "planned-cms-staging.imgix.net"
    ],
  },
};

export default nextConfig;